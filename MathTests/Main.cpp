#include <cassert>
#include <cstdio>
#include "Test.h"
#include "vec2.h"
#include <cmath>
int main()
{
	printf("Hello World! %d"), doNothing(40);

	assert((vec2{ 0,0 } == vec2{ 0,0 }));
	/*vec2 N = normal(vec2{ 1,1 });
	assert(N.x == N.y);
	assert(magnitude(N) == 1);*/

	getchar();
	return 0;
}